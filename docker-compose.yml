version: '3.9'
services:
  #bjorn run
  bjorn:
    image: andersenlabapps/outbreak-info-bjorn
    container_name: bjorn
    build:
      context: .
      dockerfile: Dockerfile-bjorn
    profiles: ["new-data", "all"]  
    volumes:
      - type: bind
        source: ./bjorn/
        target: /home/chrissy/outbreak.info/bjorn
   
  #es from docker image single node
  es:
    image: andersenlabapps/outbreak-info-es
    container_name: es
    environment:
      - node.name=es
      - discovery.seed_hosts=es02
      - cluster.initial_master_nodes=es
      - bootstrap.memory_lock:true
      - "ES_JAVA_OPTS=-Xms50g -Xmx50g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    build:
      context: .
      dockerfile: Dockerfile-es
    ports:
      - "9200:9200"
    volumes:
      - data01:/usr/share/elasticsearch/data
    networks:
      - some-net
    profiles: ["ingest-data", "website", "all", "debug"]
  es02:
    image: andersenlabapps/outbreak-info-es
    container_name: es02
    environment:
      - node.name=es02
      - discovery.seed_hosts=es
      - cluster.initial_master_nodes=es
      - bootstrap.memory_lock:true
      - "ES_JAVA_OPTS=-Xms40g -Xmx40g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    build:
      context: .
      dockerfile: Dockerfile-es
    volumes:
      - data02:/usr/share/elasticsearch/data
    networks:
      - some-net
    profiles: ["ingest-data", "website", "all", "debug"]
 
  #ingest the es db
  ingest:
    logging:
      driver: none
    image: andersenlabapps/outbreak-info-ingest
    container_name: ingest
    build:
      context: .
      dockerfile: Dockerfile-index
    volumes:
      - type: bind
        source: /home/chrissy/outbreak.info/bjorn/test/test_genomics_data.json
        target: /test_genomics_data.json
    restart: on-failure:10
    depends_on:
      - es
    networks:
      - some-net 
<<<<<<< HEAD
    profiles: ["ingest-data", "all", "debug"]
=======
    profiles: ["ingest-data", "all", "ingest-only"]
>>>>>>> f095a06430d04d5dca9c188779971d0782cc6ae1
  
  #start up the api
  tornado:
    image: andersenlabapps/outbreak-info-tornado
    container_name: tornado
    build:
      context: .
      dockerfile: Dockerfile-server
    restart: on-failure:10
    ports:
      - "8000:8000"
    networks:
      - some-net
    profiles: ["ingest-data", "website", "all"]

  #start up the local build
  localbuild:
    image: andersenlabapps/outbreak-info-website
    container_name: localbuild
    build:
      context: .
      dockerfile: Dockerfile-client  
    restart: on-failure:10
    depends_on: 
      - tornado
    networks:
      - some-net 
    ports:
      - "5000:5000"
    profiles: ["ingest-data", "website", "all"]

volumes:
  data01:
    driver: local
  data02:
    driver: local
networks:
  some-net:
    driver: bridge
      
